var Store = require("../db/store.js");

module.testAddImage = function (test) {
    test.done();
};

module.testRemoveImage = function (test) {
    test.done();
};

module.testGetImageByTag = function (test) {
    test.done();
};

module.testGetImagesByTag = function (test) {
    test.done();
};

module.testAddTags = function (test) {
    test.done();
};

module.testRemoveTags = function (test) {
    test.done();
};

module.testRemoveTags = function (test) {
    test.done();
};
